%%-*- mode: erlang -*-

{ application, jesse, [ {description, "jesse"}
                      , {vsn, git}
                      , {modules, []}
                      , {registered, []}
                      , {applications, [ kernel
                                       , stdlib
                                       ]}
                      ]}.
